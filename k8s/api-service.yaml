apiVersion: v1
kind: Service
metadata:
  name: fraud-api-service
  labels:
    app: fraud-api
spec:
  type: LoadBalancer
  selector:
    app: fraud-api
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
