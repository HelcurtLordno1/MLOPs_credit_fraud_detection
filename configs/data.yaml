project:
  name: fraud-detector
  seed: 42

data:
  train_csv: fraudTrain.csv
  test_csv: fraudTest.csv
  processed_dir: data/processed
  reports_dir: reports/metrics
  target_column: is_fraud
  index_column: row_id
  summary_name: day1_data_summary.json
  sample_rows: null

